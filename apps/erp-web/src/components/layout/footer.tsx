export function Footer() {
  return (
    <footer className="border-t border-border px-4 py-3 text-center text-xs text-muted-foreground">
      Lateen OS ERP Web — consumes real engine runtimes only. No authentication is wired up yet.
    </footer>
  );
}
