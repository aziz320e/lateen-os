/**
 * Authentication — Token validation, Session validation, and API Key
 * validation, composed over the real Identity service.
 * @module authentication
 */
export { createAuthenticationService, type AuthenticationService } from './service.impl.js';
