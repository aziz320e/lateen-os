/**
 * Timestamp primitives for domain modeling.
 *
 * @module time/timestamp
 */

/** ISO 8601 date-time string in UTC (e.g. `2026-07-18T18:00:00.000Z`). */
export type Timestamp = string;

/** ISO 8601 calendar date string (YYYY-MM-DD). */
export type DateOnly = string;
